-- Entry point. This is the URL you paste.
-- Chain: 1x1x1 -> 1x1x -> 1x1 -> 1x1x1x (the library itself)
--
-- The 'return' is load-bearing. Without it this hands back nil and the whole
-- chain silently fails, which is the usual reason a setup like this breaks.
return loadstring(game:HttpGet("https://raw.githubusercontent.com/aparaanana-hue/DW/refs/heads/main/chain/1x1x"))()
